"""Source-style adapter namespace for RCCKM ingest parsing.

The current parser uses deterministic pattern groups plus generic fallback.
These modules are intentionally lightweight extension points for future
source-specific label variants.
"""

